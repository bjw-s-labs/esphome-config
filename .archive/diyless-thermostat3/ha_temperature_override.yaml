---
substitutions:
  # The input temperature sensor in HA.
  temperature_sensor_ha_entity_id: ""

sensor:
  - id: external_sensor
    platform: homeassistant
    internal: true
    entity_id: ${temperature_sensor_ha_entity_id}

climate:
  - id: !extend ch_climate
    sensor: external_sensor
